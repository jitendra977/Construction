# Django Settings
SECRET_KEY=generate-a-secure-secret-key-for-production
DEBUG=False
ALLOWED_HOSTS=localhost,127.0.0.1,your-production-domain.com

# Database (SQLite for development, PostgreSQL for production)
# Development:
DATABASE_URL=sqlite:///db.sqlite3
# Production (use the service name 'db' as defined in docker-compose):
# DATABASE_URL=postgres://construction_user:construction_password@db:5432/construction_db

# CORS Settings
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,https://your-production-domain.com

# JWT Settings
JWT_ACCESS_TOKEN_LIFETIME=60
JWT_REFRESH_TOKEN_LIFETIME=1440
