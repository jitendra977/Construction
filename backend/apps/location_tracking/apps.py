from django.apps import AppConfig

class LocationTrackingConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.location_tracking'
    verbose_name = 'Location Tracking'
