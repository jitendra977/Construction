version: '3.8'

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: construction_backend
    volumes:
      - media_data:/app/media
    ports:
      - "8001:8000" # Changed to 8001 to avoid conflict with existing services
    env_file:
      - ./backend/.env
    # db is external (mysql_db) on 'app-network'
    networks:
      - construction_network
      - app_network

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: construction_frontend
    volumes:
      - media_data:/usr/share/nginx/html/media
    ports:
      - "8080:80" # Changed to 8080 to avoid conflict with Proxy Manager on port 80
    depends_on:
      - backend
    networks:
      - construction_network
      - app_network

networks:
  construction_network:
    driver: bridge
  app_network:
    external: true
    name: app-network

volumes:
  media_data:
