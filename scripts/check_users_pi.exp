#!/usr/bin/expect -f
set timeout 10
spawn ssh -o StrictHostKeyChecking=no jitendra@192.168.0.59 "cd ~/Construction && docker compose exec backend python manage.py shell -c \"from django.contrib.auth import get_user_model; User = get_user_model(); print(f'Users: {User.objects.count()}'); print([u.username for u in User.objects.all()])\""
expect {
    "password:" {
        send "jitu9844\r"
        exp_continue
    }
    eof
}
